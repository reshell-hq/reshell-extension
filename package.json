{
  "name": "reshell-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "dev:firefox": "wxt -b firefox",
    "bundle-reshell": "node scripts/bundle-reshell.mjs",
    "build": "node scripts/bundle-reshell.mjs && wxt build",
    "build:extension-only": "wxt build",
    "build:firefox": "node scripts/bundle-reshell.mjs && wxt build -b firefox",
    "zip": "wxt zip",
    "zip:firefox": "wxt zip -b firefox"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "typescript": "^5.8.0",
    "wxt": "^0.20.0"
  }
}